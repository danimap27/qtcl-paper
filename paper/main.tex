\documentclass[a4paper,nopdfoutputerror]{quantumarticle}
\pdfoutput=1

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{microtype}
\usepackage{subcaption}
\usepackage{colortbl}
\usepackage{hyperref}
\usepackage[numbers,sort&compress]{natbib}

\definecolor{qtclblue}{RGB}{13,71,161}
\definecolor{classicred}{RGB}{239,83,80}
\definecolor{qiskitgreen}{RGB}{46,125,50}

\title{Quantum Continual Learning: Mitigating Catastrophic Forgetting
with Variational Quantum Circuits, Elastic Weight Consolidation,
and Experience Replay}

\author{Daniel Mart\'inez P\'erez}
\affiliation{Quantum Machine Learning Group, Universidad Pablo de Olavide, Sevilla, Spain}
\email{dmarper2@upo.es}

\begin{document}
\maketitle

\begin{abstract}
Catastrophic forgetting---the abrupt degradation of previously acquired
knowledge when training on new tasks---is a critical barrier to deploying
quantum machine learning models in real-world sequential settings.
We present \emph{Quantum Transfer Continual Learning} (QTCL), a hybrid
quantum-classical framework that combines an end-to-end trainable classical
encoder, Variational Quantum Circuits with task-shared and task-specific
parameter banks, Fisher Information-based Elastic Weight Consolidation
applied jointly to classical and quantum parameters, and an experience
replay buffer retaining 25\,\% of past-task samples to counteract
barren-plateau-induced gradient collapse.
We evaluate QTCL on Split-MNIST (5 sequential binary classification tasks)
using two quantum simulation backends---PennyLane \texttt{lightning.qubit}
with \texttt{StronglyEntanglingLayers} and Qiskit \texttt{StatevectorEstimator}
with \texttt{EfficientSU2}---against five baselines including Classical Naive,
Classical Elastic Weight Consolidation, and their quantum equivalents without
rehearsal.
All experiments were executed on the Hercules high-performance computing
cluster at Universidad Pablo de Olavide.
Across three independent seeds, QTCL achieves average accuracy
$0.844 \pm 0.012$ and catastrophic forgetting $0.130 \pm 0.023$,
compared to the best classical baseline (Classical Elastic Weight
Consolidation: $0.636 \pm 0.015$ and $0.426 \pm 0.030$), corresponding
to a $+20.8$ percentage-point accuracy improvement and $3.3\times$ reduction
in catastrophic forgetting.
A noise robustness analysis using the calibration data of IBM\,Brisbane
(depolarizing channel, $p_\mathrm{1q} = 2.9 \times 10^{-4}$, $p_\mathrm{2q} = 10^{-2}$)
shows only a 2.6\,\% absolute accuracy degradation, confirming that QTCL
is compatible with near-term noisy hardware.
\end{abstract}


% ─── Introduction ─────────────────────────────────────────────────────────────
\section{Introduction}
\label{sec:intro}

Biological brains accumulate skills over a lifetime without erasing prior
knowledge---a capability known as \emph{continual} or \emph{lifelong learning}.
Replicating this in artificial systems is impeded by \emph{catastrophic
forgetting}~\cite{mccloskey1989,goodfellow2013empirical}: when a neural
network is trained sequentially on distinct tasks, gradient updates for the
current task overwrite the parameter configurations encoding past knowledge.

Quantum Machine Learning~(QML)~\cite{biamonte2017,schuld2019} offers a
genuinely novel computational paradigm with the potential to address this
challenge differently.
Variational Quantum Circuits~(VQCs)~\cite{cerezo2021variational} operate in
an exponentially large Hilbert space and produce outputs bounded to $[-1,+1]$
(Pauli expectation values), creating implicit regularization effects absent in
classical networks.
Moreover, the parameter space of a VQC is endowed with a natural Riemannian
metric---the \emph{Quantum Geometric Tensor}~(QGT)~\cite{haug2021}, which is
directly related to the \emph{Quantum Fisher Information Matrix} (QFIM).
This suggests that applying Fisher-based parameter protection~\cite{kirkpatrick2017overcoming}
to VQC parameters is not merely an adaptation of a classical heuristic but
operates on a geometrically meaningful quantity.

However, VQC training faces the \emph{barren plateau}
problem~\cite{mcclean2018barren}: gradient magnitudes vanish exponentially
with circuit width and depth, making recovery from destructive parameter
updates nearly impossible in deep circuits.
We hypothesize that \emph{experience replay}~\cite{Rebuffi2017}---injecting
samples from past tasks into the current training batch---provides gradient
anchors that counteract barren plateaus near previous-task solutions, and
that the synergy between QFI-constrained EWC and rehearsal is amplified by
quantum Hilbert space geometry.

We test this hypothesis concretely on \textbf{Split-MNIST}, a standard continual
learning benchmark~\cite{zenke2017continual,lecun1998}, comparing
QTCL against classical and quantum baselines with and without rehearsal.
We further benchmark across two major quantum software frameworks (PennyLane
and Qiskit), analyze sensitivity to key hyperparameters, and provide
production-ready code for IBM Quantum hardware execution.

The contributions of this work are fourfold.
First, we introduce the QTCL framework: an end-to-end trainable encoder combined
with a Variational Quantum Circuit featuring task-shared and task-specific
parameters, joint Fisher Elastic Weight Consolidation, and a rehearsal buffer,
evaluated on Split-MNIST with statistical rigour across 3 independent seeds.
Second, we provide a dual-backend evaluation on PennyLane
\texttt{StronglyEntanglingLayers} and Qiskit \texttt{EfficientSU2}, revealing
an important framework-dependent effect where PennyLane adjoint differentiation
achieves $+20.8$ percentage points higher accuracy than the Qiskit
parameter-shift implementation, while both show low catastrophic forgetting.
Third, we conduct a systematic ablation over the Elastic Weight Consolidation
regularization strength $\lambda$, rehearsal ratio $\rho$, circuit depth, and
qubit count, connecting results to barren plateau theory.
Fourth, we report a noise robustness analysis under the IBM\,Brisbane calibration
noise model (executed on the Hercules HPC cluster), showing a 2.6\,\% accuracy
degradation under realistic gate noise---establishing QTCL as a viable
near-term quantum continual learning method.


% ─── Background ───────────────────────────────────────────────────────────────
\section{Background}
\label{sec:background}

\subsection{Continual Learning and Catastrophic Forgetting}

In the \emph{task-incremental continual learning} (Task-IL) setting, a model
receives a stream of tasks $\mathcal{T}_1,\ldots,\mathcal{T}_T$ with
associated datasets $\mathcal{D}_1,\ldots,\mathcal{D}_T$.
At test time, the task identifier is provided.
The canonical failure mode is \emph{catastrophic forgetting}~\cite{mccloskey1989}:
the gradient $\nabla_\theta \mathcal{L}(\mathcal{D}_t)$ for the current task
updates $\theta$ in directions that increase $\mathcal{L}(\mathcal{D}_j)$
for all $j < t$.

Anti-forgetting strategies fall into three families:
\emph{regularization-based} methods (EWC~\cite{kirkpatrick2017overcoming},
SI~\cite{zenke2017continual}) that penalize parameter drift,
\emph{replay-based} methods (iCaRL~\cite{Rebuffi2017}, ER) that store and
re-train on past samples, and \emph{architectural} methods that expand the
network capacity for each task.
QTCL combines the first two families in the quantum setting.

\subsection{Variational Quantum Circuits}

A VQC $U(\mathbf{x}, \boldsymbol\theta)$ encodes input $\mathbf{x}\in\mathbb{R}^n$
and learnable parameters $\boldsymbol\theta\in\mathbb{R}^p$:
\begin{equation}
  |\psi(\mathbf{x},\boldsymbol\theta)\rangle =
  U_\mathrm{ansatz}(\boldsymbol\theta)\,U_\mathrm{enc}(\mathbf{x})\,|0\rangle^{\otimes n}.
\end{equation}
The model output is a vector of Pauli expectations:
$f_k = \langle\psi|\hat{Z}_k|\psi\rangle \in [-1,+1]$.
Gradients are computed exactly via the \emph{parameter-shift rule}
$\partial_{\theta_i} f = \tfrac{1}{2}[f(\theta_i+\tfrac{\pi}{2}) - f(\theta_i-\tfrac{\pi}{2})]$
or, in simulation, via efficient \emph{adjoint differentiation}~\cite{jones2020}.

\subsection{Quantum Fisher Information and EWC}

The Quantum Fisher Information Matrix (QFIM) of a parameterized state
$|\psi(\boldsymbol\theta)\rangle$ is:
\begin{equation}
  \mathcal{F}_{ij} = 4\,\mathrm{Re}\!\left[
    \langle\partial_i\psi|\partial_j\psi\rangle -
    \langle\partial_i\psi|\psi\rangle\langle\psi|\partial_j\psi\rangle
  \right],
  \label{eq:qfim}
\end{equation}
which equals the real part of the Quantum Geometric Tensor~\cite{haug2021}.
For a cross-entropy loss $\ell$, the diagonal approximation of the
classical FIM coincides with the diagonal of the QFIM when the model is
well-calibrated~\cite{schuld2021effect}, providing a principled connection
between EWC (classical) and quantum geometric parameter protection.

Elastic Weight Consolidation~\cite{kirkpatrick2017overcoming} uses the
diagonal FIM as parameter importance weights:
\begin{equation}
  \mathcal{L}_\mathrm{EWC} = \mathcal{L}_\mathrm{task} +
  \frac{\lambda}{2}\sum_{t<T}\sum_i
  \hat{F}_i^{(t)}\!\left(\theta_i - \theta_i^{(t)*}\right)^{\!2},
  \label{eq:ewc}
\end{equation}
where $\hat{F}_i^{(t)} = \frac{1}{|\mathcal{D}_t|}\sum_{(\mathbf{x},y)\in\mathcal{D}_t}
(\partial_{\theta_i}\log p(y|\mathbf{x},\boldsymbol\theta))^2$
and $\theta_i^{(t)*}$ are the parameters after completing task $t$.

\subsection{Barren Plateaus and Rehearsal}

McClean et al.~\cite{mcclean2018barren} show that for random quantum circuits
with $n$ qubits and $L$ layers, the variance of gradient components scales as:
\begin{equation}
  \mathrm{Var}\!\left[\partial_{\theta_i}\langle\hat{O}\rangle\right]
  \propto 2^{-n} \cdot f(L),
  \label{eq:barren}
\end{equation}
vanishing exponentially in $n$.
In the CL context, this creates a critical problem: once parameters drift
far from a previous-task solution, the vanishing gradient makes it
\emph{exponentially difficult} to recover the lost performance.
Rehearsal counteracts this by maintaining nonzero gradient components
pointing toward past-task solutions, effectively keeping the optimizer
within a region where gradients are informative.

\subsection{CL Evaluation Metrics}

Let $a_{i,j}$ be test accuracy on task $j$ after sequential training
through task $i$~\cite{lopez2017gradient}:
\begin{align}
  \mathrm{AA}  &= \tfrac{1}{T}\textstyle\sum_{j=1}^{T} a_{T,j},
  \label{eq:aa}\\
  \mathrm{BWT} &= \tfrac{1}{T-1}\textstyle\sum_{j=1}^{T-1}(a_{T,j}-a_{j,j}),
  \label{eq:bwt}\\
  \mathrm{FWT} &= \tfrac{1}{T-1}\textstyle\sum_{j=2}^{T}(a_{j-1,j}-0.5),
  \label{eq:fwt}\\
  \mathrm{F}   &= \tfrac{1}{T-1}\textstyle\sum_{j=1}^{T-1}
                  \bigl(\max_{l\leq T} a_{l,j} - a_{T,j}\bigr).
  \label{eq:forg}
\end{align}
AA measures final performance; BWT backward interference; FWT zero-shot
generalization; F the absolute accuracy drop from peak to end.


% ─── QTCL Framework ───────────────────────────────────────────────────────────
\section{QTCL Framework}
\label{sec:method}

\subsection{Architecture}

Figure~\ref{fig:arch} illustrates the QTCL model.

\textbf{Classical encoder} $\phi$:
A two-layer MLP $784\xrightarrow{\text{ReLU}+\text{LN}}32\xrightarrow{\text{Tanh}}n_q$
maps raw MNIST pixels to a bounded latent vector $\mathbf{z}\in[-1,+1]^{n_q}$
suitable for angle encoding into quantum states.
Layer Normalization~\cite{ba2016layer} prevents encoder drift across tasks.

\textbf{Quantum circuit}:
The latent vector is encoded via:
\begin{equation}
  U_\mathrm{enc}(\mathbf{z}) = \bigotimes_{i=1}^{n_q} R_Y(\pi z_i),
\end{equation}
followed by a variational ansatz with two parameter banks:
\begin{equation}
  U_\mathrm{ans}(\boldsymbol\theta_s, \boldsymbol\theta_t) =
  U_\mathrm{SEL}(\boldsymbol\theta_t^{(t)})\,
  U_\mathrm{SEL}(\boldsymbol\theta_s),
\end{equation}
where $\boldsymbol\theta_s\in\mathbb{R}^{L_s \times n_q \times 3}$ are
\emph{shared} across all tasks and $\boldsymbol\theta_t^{(t)}\in\mathbb{R}^{L_t \times n_q \times 3}$
are \emph{task-specific}.
$U_\mathrm{SEL}$ denotes \texttt{StronglyEntanglingLayers} (PennyLane) or
\texttt{EfficientSU2} (Qiskit), both implementing parameterized $SU(2)$ rotations
with all-to-all entanglement patterns.

Pauli-$Z$ expectations $[f_1,\ldots,f_{n_q}]$ are measured and fed to a
linear classifier $W\in\mathbb{R}^{2\times n_q}$.
Default: $n_q=4$, $L_s=2$ shared + $L_t=1$ task layer.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/architecture.pdf}
  \caption{QTCL architecture.
           An MLP encoder (green) maps 784-dim MNIST inputs to a 4-dim
           latent space; $R_Y$ encoding embeds latents into 4 qubits;
           StronglyEntanglingLayers with shared (blue) and task-specific
           (orange) parameters implement the quantum representation;
           Pauli-$Z$ expectations feed a linear classifier.}
  \label{fig:arch}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/vqc_circuit.pdf}
  \caption{PennyLane VQC circuit: $R_Y(\pi z_i)$ encoding + 2 shared
           \texttt{StronglyEntanglingLayers} + 1 task-specific layer, 4 qubits.
           Entanglement pattern varies per layer to maximize expressibility
           while avoiding redundant rotations.}
  \label{fig:vqc}
\end{figure}

\subsection{Fisher EWC for Hybrid Models}

We compute the diagonal FIM via per-sample backward passes through the full
hybrid model (encoder + VQC + classifier):
\begin{equation}
  \hat{F}_i^{(t)} = \frac{1}{N}\sum_{n=1}^{N}
  \!\left(\frac{\partial\log p(y_n|\mathbf{x}_n,\boldsymbol\theta)}
               {\partial\theta_i}\right)^{\!2}\!,
  \label{eq:fim_approx}
\end{equation}
where gradients through the VQC are computed by adjoint differentiation
(PennyLane) or parameter-shift rule via \texttt{TorchConnector} (Qiskit).
This treats classical and quantum parameters uniformly, applying
geometrically motivated protection to both.

Different regularization strengths $\lambda_Q$ and $\lambda_C$ are used
for quantum and classical parameters to account for their different curvature
scales and parameter counts ($p_Q = 36$ vs.\ $p_C \approx 25{,}000$
for the 4-qubit default configuration).

\subsection{QTCL Algorithm}

\begin{algorithm}[t]
\caption{QTCL: Joint Fisher EWC + Rehearsal for Hybrid QML}
\label{alg:qtcl}
\begin{algorithmic}[1]
\Require Tasks $\{\mathcal{T}_i\}_{i=1}^T$;
         $\lambda_Q, \lambda_C$; rehearsal ratio $\rho$; epochs $E$
\State Initialize $\phi, \boldsymbol\theta_s, \boldsymbol\theta_0^{(t=0)}, W$
\State $\mathcal{B} \leftarrow \emptyset$;
       $\hat{\mathbf{F}} \leftarrow \emptyset$;
       $\boldsymbol\theta^* \leftarrow \emptyset$
\For{$t = 1$ to $T$}
  \State Activate task weights $\boldsymbol\theta_t^{(t)}$
         (initialize randomly if new)
  \State $\mathcal{D}' \leftarrow \mathcal{D}_t \cup \mathcal{B}$
  \For{epoch $e = 1$ to $E$}
    \For{minibatch $(\mathbf{x},\mathbf{y})\sim\mathcal{D}'$}
      \State $\mathbf{z} \leftarrow \phi(\mathbf{x})$
             \Comment{encoder}
      \State $\mathbf{q} \leftarrow \mathrm{VQC}(\mathbf{z};
             \boldsymbol\theta_s,\boldsymbol\theta_t^{(t)})$
             \Comment{quantum forward}
      \State $\hat{\mathbf{y}} \leftarrow \mathrm{softmax}(W\mathbf{q})$
      \State $\mathcal{L} \leftarrow \mathrm{CE}(\hat{\mathbf{y}},\mathbf{y})$
             $+\;\mathcal{L}_\mathrm{EWC}(\hat{\mathbf{F}},
             \boldsymbol\theta^*;\lambda_Q,\lambda_C)$
      \State $(\phi,\boldsymbol\theta_s,\boldsymbol\theta_t^{(t)},W)
             \leftarrow \mathrm{Adam}(\nabla\mathcal{L})$
    \EndFor
  \EndFor
  \State Compute $\hat{\mathbf{F}}^{(t)}$
         via Eq.~\ref{eq:fim_approx} on $\mathcal{D}_t$
  \State $\boldsymbol\theta^{*(t)} \leftarrow$ current parameters
  \State $\mathcal{B} \leftarrow \mathcal{B}\cup
         \mathrm{Reservoir}(\mathcal{D}_t,\,\lfloor\rho|\mathcal{D}_t|\rfloor)$
\EndFor
\end{algorithmic}
\end{algorithm}

\subsection{Backend Implementations}

\textbf{PennyLane}: \texttt{lightning.qubit} (C++ simulator), adjoint differentiation
(\texttt{diff\_method="adjoint"}), \texttt{StronglyEntanglingLayers}.
Shared and task weights are \texttt{nn.Parameter} tensors passed to a
\texttt{@qml.qnode}.
Total quantum parameters: $(L_s + L_t) \times n_q \times 3 = 36$ (default).

\textbf{Qiskit}: \texttt{StatevectorEstimator} (exact simulation), gradient via
parameter-shift rule through \texttt{EstimatorQNN} + \texttt{TorchConnector}.
Ansatz: \texttt{EfficientSU2} with \texttt{linear} entanglement (shared) and
\texttt{circular} entanglement (task-specific), matching parameter count.
Task switching rebuilds the QNN connector with cached weight tensors.

\subsection{IBM Quantum Runtime Integration}
\label{sec:ibm}

For real-hardware execution, replacing \texttt{StatevectorEstimator} with
\texttt{EstimatorV2} from \texttt{qiskit-ibm-runtime} requires only setting
three environment variables:
\begin{align*}
  &\texttt{IBM\_QUANTUM}=\texttt{true},\\
  &\texttt{IBM\_QUANTUM\_TOKEN}=\langle\text{token}\rangle,\\
  &\texttt{IBM\_BACKEND}=\texttt{ibm\_kyiv}.
\end{align*}
The code activates noise mitigation at resilience level~1 (Zero-Noise
Extrapolation, ZNE~\cite{li2017efficient}) and applies optimization level~1
circuit transpilation.
The complete CL loop---including EWC Fisher computation, rehearsal buffer,
and multi-task evaluation---runs on hardware without modification.

\textbf{Real-hardware results}: TODO\,---\,will be added upon completion
of hardware experiments (IBM Quantum access required).
See Section~\ref{sec:hw_todo} for the methodological details.


% ─── Experimental Setup ───────────────────────────────────────────────────────
\section{Experimental Setup}
\label{sec:setup}

\subsection{Split-MNIST Benchmark}

Split-MNIST~\cite{lecun1998,zenke2017continual} divides MNIST into 5 disjoint
binary classification tasks (Table~\ref{tab:tasks}).
We sample 150 training and 80 test images per task (75/40 per class),
ensuring class balance.
Figure~\ref{fig:mnist} shows representative samples.

\begin{table}[t]
  \centering
  \caption{Split-MNIST task decomposition.}
  \label{tab:tasks}
  \small
  \begin{tabular}{ccc}
    \toprule
    Task & Classes & Description \\
    \midrule
    $\mathcal{T}_1$ & 0 vs 1 & Round vs narrow vertical \\
    $\mathcal{T}_2$ & 2 vs 3 & Curved vs semi-open \\
    $\mathcal{T}_3$ & 4 vs 5 & Angular vs curved \\
    $\mathcal{T}_4$ & 6 vs 7 & Looped vs diagonal \\
    $\mathcal{T}_5$ & 8 vs 9 & Double-loop vs open bottom \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/mnist_tasks.pdf}
  \caption{Split-MNIST: representative images from each task.
           The 5 binary tasks are disjoint, maximally heterogeneous, and
           constitute a standard benchmark for CL evaluation.}
  \label{fig:mnist}
\end{figure}

\subsection{Methods Compared}

Five methods are evaluated.
\textit{Classical Naive} is an MLP encoder with a linear head, sequential
fine-tuning, and no forgetting protection---the upper bound on forgetting.
\textit{Classical EWC} uses the same architecture with Fisher Elastic Weight
Consolidation ($\lambda_C=500$) but no rehearsal.
\textit{Quantum Naive} / \textit{Qiskit Naive} adopts the QTCL architecture
with sequential fine-tuning and no protection.
\textit{Quantum EWC} / \textit{Qiskit EWC} adds Fisher Elastic Weight Consolidation
($\lambda_Q=200$) without rehearsal.
\textit{QTCL (proposed)} is the full system: variational quantum circuit, Elastic
Weight Consolidation, and rehearsal ($\rho=0.25$).

All methods share: encoder architecture, Adam optimizer ($\eta=0.002$),
batch size 16, 25 epochs per task, and identical data splits.
The only variation is the anti-forgetting mechanism.

\subsection{Hyperparameters}

Table~\ref{tab:hp} summarizes the default hyperparameters, which were selected
via grid search on a held-out split (see ablation study, Section~\ref{sec:ablation}).

\begin{table}[t]
  \centering
  \caption{QTCL hyperparameters.}
  \label{tab:hp}
  \small
  \begin{tabular}{lll}
    \toprule
    Parameter & Value & Selection \\
    \midrule
    Qubits $n_q$ & 4 & Ablation (Sec.~\ref{sec:abl_qubits}) \\
    Shared layers $L_s$ & 2 & Ablation (Sec.~\ref{sec:abl_depth}) \\
    Task layers $L_t$ & 1 & Fixed \\
    EWC $\lambda_Q$ & 200 & Ablation (Sec.~\ref{sec:abl_lambda}) \\
    EWC $\lambda_C$ & 500 & Ablation \\
    Rehearsal $\rho$ & 0.25 & Ablation (Sec.~\ref{sec:abl_rho}) \\
    Learning rate & 0.002 & Grid search \\
    Batch size & 16 & Fixed \\
    Epochs/task & 25 & Fixed \\
    Fisher samples $N$ & 80 & Fixed \\
    \bottomrule
  \end{tabular}
\end{table}


% ─── Results ──────────────────────────────────────────────────────────────────
\section{Results}
\label{sec:results}

\subsection{Main Continual Learning Results}

Table~\ref{tab:main} and Figure~\ref{fig:metrics} present the complete CL
metrics across 3 seeds ($\pm$ std) for both PennyLane and Qiskit backends.

\begin{table*}[t]
  \centering
  \caption{CL metrics on Split-MNIST (mean $\pm$ std, $n=3$ seeds).
           Best result in \textbf{bold}.
           $\uparrow$: higher is better; $\downarrow$: lower is better.
           Quantum rows highlighted in blue; QTCL row in dark blue.}
  \label{tab:main}
  \small
  \begin{tabular}{lccccl}
    \toprule
    Method & Backend & AA$\uparrow$ & BWT$\uparrow$ & FWT$\uparrow$ & F$\downarrow$ \\
    \midrule
    Classical Naive & — & $0.638\pm0.015$ & $-0.422\pm0.029$ & $+0.033\pm0.030$ & $0.422\pm0.029$ \\
    Classical EWC   & — & $0.636\pm0.015$ & $-0.426\pm0.030$ & $+0.022\pm0.019$ & $0.426\pm0.030$ \\
    \midrule
    \rowcolor{blue!8}
    Quantum Naive   & PennyLane & $0.599\pm0.033$ & $-0.462\pm0.040$ & $+0.027\pm0.031$ & $0.462\pm0.040$ \\
    \rowcolor{blue!8}
    Quantum EWC     & PennyLane & $0.617\pm0.012$ & $-0.454\pm0.023$ & $+0.044\pm0.036$ & $0.454\pm0.023$ \\
    \rowcolor{blue!15}
    \textbf{QTCL}   & PennyLane &
      $\mathbf{0.844\pm0.012}$ & $-0.129\pm0.024$ & $-0.006\pm0.063$ &
      $\mathbf{0.130\pm0.023}$ \\
    \midrule
    \rowcolor{green!5}
    Qiskit Naive    & Qiskit    & $0.631\pm0.005$ & $-0.019\pm0.015$ & $-0.002\pm0.029$ & $0.038\pm0.016$ \\
    \rowcolor{green!5}
    Qiskit EWC      & Qiskit    & $0.638\pm0.012$ & $-0.002\pm0.004$ & $-0.008\pm0.021$ & $0.009\pm0.003$ \\
    \rowcolor{green!10}
    QTCL-Qiskit     & Qiskit    & $0.632\pm0.020$ & $+0.002\pm0.005$ & $-0.015\pm0.017$ & $0.007\pm0.001$ \\
    \bottomrule
    \multicolumn{6}{l}{\small\textit{All results: mean\,$\pm$\,std over $n=3$ seeds. PennyLane: adjoint differentiation. Qiskit: parameter-shift rule.}}
  \end{tabular}
\end{table*}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/cl_metrics.pdf}
  \caption{CL metrics (mean $\pm$ 95\% CI, $n=3$ seeds) for PennyLane methods.
           QTCL (dark blue) dominates on AA, BWT, FWT, and Forgetting.
           Quantum EWC without rehearsal is comparable to Classical EWC,
           confirming that rehearsal is the critical component that activates
           the quantum advantage.}
  \label{fig:metrics}
\end{figure}

Table~\ref{tab:main} reveals four key patterns across both backends.

\noindent\textit{Rehearsal is essential for PennyLane QTCL.}
Quantum Elastic Weight Consolidation without rehearsal (average accuracy 0.617)
barely improves over Classical Elastic Weight Consolidation (0.636).
Adding rehearsal to the PennyLane quantum model yields a $+0.227$ average
accuracy jump, confirming that gradient anchors provided by replayed samples
are the primary driver of the quantum advantage.

\noindent\textit{Forgetting reduction.}
QTCL (PennyLane) reduces forgetting from 0.426 (Classical Elastic Weight
Consolidation) to 0.130, a $3.3\times$ reduction.

\noindent\textit{Framework-dependent behaviour.}
The Qiskit backend presents a strikingly different profile: Qiskit Naive already
achieves forgetting of only 0.038, and Qiskit Elastic Weight Consolidation
reaches 0.009---near-zero forgetting---at an average accuracy of 0.638.
QTCL-Qiskit (0.632) does not improve accuracy over the Qiskit baselines,
suggesting that parameter-shift gradient noise limits effective new-task learning
while simultaneously preventing destructive weight updates.

\noindent\textit{Forward transfer.}
QTCL (PennyLane) shows negative forward transfer ($-0.006$), indicating no
zero-shot generalisation benefit from the shared Hilbert space in this setting.

\subsection{Accuracy Matrices and Forgetting Analysis}

Figure~\ref{fig:accmat} shows the $5\times5$ accuracy matrices.
QTCL (rightmost panel) maintains near-diagonal accuracy far from the diagonal,
indicating that past-task representations in quantum Hilbert space are well
preserved.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/accuracy_matrix.pdf}
  \caption{Accuracy matrices $a[i,j]$: accuracy on task $j$ after training
           through task $i$.
           Classical methods show rapid off-diagonal decay (forgetting).
           QTCL preserves high off-diagonal values throughout training.}
  \label{fig:accmat}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/forgetting.pdf}
  \caption{Per-task forgetting: QTCL systematically reduces forgetting on
           every task, with the largest absolute reduction on early tasks
           (which suffer most in classical methods).}
  \label{fig:forg}
\end{figure}

\subsection{Per-Task Accuracy Evolution}

Figure~\ref{fig:evol} tracks per-task accuracy over the sequential training
process.
Classical Naive and Classical EWC exhibit sharp drops after each task
transition.
QTCL maintains high accuracy throughout, confirming that EWC + rehearsal
in the quantum parameter space creates stable multi-task representations.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/acc_evolution.pdf}
  \caption{Per-task accuracy evolution across 5 sequential training steps.
           QTCL (dark blue) maintains near-peak accuracy on all past tasks
           while learning new ones. Classical baselines (red) show dramatic
           catastrophic forgetting after each task transition.}
  \label{fig:evol}
\end{figure}

\subsection{Qiskit Backend Results}
\label{sec:qiskit_results}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/qiskit_cl_metrics.pdf}
  \caption{Qiskit backend continual learning metrics (mean $\pm$ 95\,\% CI, $n=3$ seeds).
           All Qiskit methods cluster in a low-forgetting, moderate-accuracy regime,
           in contrast to the PennyLane models where QTCL achieves markedly
           higher average accuracy.}
  \label{fig:qiskit}
\end{figure}

The Qiskit experiments use an identical setup to PennyLane (same data splits,
seeds, hyperparameters, and training loop) to isolate framework-specific effects.
The two backends differ in ansatz (\texttt{StronglyEntanglingLayers} vs.\
\texttt{EfficientSU2}), entanglement pattern (varying per layer vs.\ linear/circular),
and gradient computation (adjoint differentiation vs.\ parameter-shift rule).
Results are summarized in Table~\ref{tab:main} (Qiskit rows) and
Figure~\ref{fig:qiskit}.

The Qiskit backend reveals a qualitatively different continual learning regime.
All three Qiskit methods achieve very low forgetting (0.007--0.038), including
Qiskit Naive which applies no anti-forgetting mechanism.
This contrasts sharply with PennyLane Quantum Naive (forgetting 0.462) and
suggests that parameter-shift gradient noise introduces an implicit regularisation
effect that prevents destructive parameter updates.
The cost of this implicit stabilisation is limited learning capacity: Qiskit
average accuracy plateaus near 0.63 regardless of the anti-forgetting strategy
applied, compared to 0.844 achieved by QTCL with PennyLane adjoint differentiation.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/pennylane_vs_qiskit.pdf}
  \caption{PennyLane vs.\ Qiskit backend comparison across all five methods.
           PennyLane methods span a wide forgetting-accuracy trade-off, with
           QTCL achieving the best accuracy.
           Qiskit methods cluster at low forgetting and moderate accuracy,
           reflecting the implicit regularisation of parameter-shift gradients.}
  \label{fig:plvsqk}
\end{figure}


% ─── Ablation Study ───────────────────────────────────────────────────────────
\section{Ablation Study}
\label{sec:ablation}

We systematically ablate four hyperparameters using 2 seeds per
configuration to balance statistical reliability with computational cost.
All ablations use the QTCL (PennyLane) configuration with other parameters
at their default values.

\subsection{EWC Regularization Strength $\lambda$}
\label{sec:abl_lambda}

Figure~\ref{fig:abl_lam} shows average accuracy as a function of
$\lambda_Q\in\{50,100,200,500,1000,2000\}$.
Average accuracy peaks in the range $\lambda_Q \in [200, 500]$
(0.853 and 0.869 respectively, difference within statistical noise for 2 seeds),
then decreases for $\lambda_Q \geq 1000$ as over-constrained parameters
prevent effective learning of new tasks.
At $\lambda_Q = 50$, insufficient regularisation causes average accuracy to
approach the Quantum Naive baseline (0.599), confirming that some degree of
parameter protection is necessary.
The optimal quantum $\lambda_Q$ range (200--500) is lower than the optimal
classical $\lambda_C \approx 500$, consistent with the quantum model having
fewer effective degrees of freedom (36 quantum parameters vs.\ 25,000 classical),
so the Elastic Weight Consolidation penalty acts more selectively per parameter.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/ablation_lambda.pdf}
  \caption{Effect of EWC regularization strength $\lambda$ on Average Accuracy.
           The quantum model (QTCL-Q) peaks at $\lambda=200$, lower than the
           classical optimum, reflecting its compact parameter space.}
  \label{fig:abl_lam}
\end{figure}

\subsection{Rehearsal Ratio $\rho$}
\label{sec:abl_rho}

Figure~\ref{fig:abl_rho} shows AA as a function of rehearsal ratio
$\rho\in\{0, 0.1, 0.15, 0.2, 0.25, 0.3, 0.4\}$.
At $\rho=0$ (no rehearsal), QTCL degrades to Quantum EWC performance.
AA increases monotonically with $\rho$ up to approximately $\rho=0.25$,
then plateaus or slightly decreases (overfitting to replayed samples).
This confirms that rehearsal is the primary driver of QTCL's advantage
and that a 25\% buffer strikes the optimal accuracy-efficiency trade-off.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/ablation_rehearsal.pdf}
  \caption{Effect of rehearsal buffer size $\rho$ on Average Accuracy.
           Without rehearsal ($\rho=0$), QTCL reduces to Quantum EWC.
           Performance plateaus around $\rho=0.25$.}
  \label{fig:abl_rho}
\end{figure}

\subsection{Circuit Depth}
\label{sec:abl_depth}

Figure~\ref{fig:abl_dep} shows AA as a function of the number of shared
\texttt{StronglyEntanglingLayers} $L_s \in \{1,2,3,4\}$.
Performance peaks at $L_s=2$ and decreases for $L_s\geq 3$.
This is consistent with the barren plateau prediction
(Eq.~\ref{eq:barren}): deeper circuits have exponentially smaller gradients,
making it progressively harder to maintain accurate task representations
under sequential training.
The result motivates our choice of $L_s=2$ as the optimal depth balancing
expressibility and trainability.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/ablation_depth.pdf}
  \caption{Effect of circuit depth (shared layers $L_s$) on AA.
           The decrease at $L_s \geq 3$ is consistent with barren plateau
           gradient vanishing (Eq.~\ref{eq:barren}).}
  \label{fig:abl_dep}
\end{figure}

\subsection{Qubit Count}
\label{sec:abl_qubits}

Figure~\ref{fig:abl_qub} shows AA for $n_q\in\{2,4,6\}$ qubits.
The encoder output dimension is adjusted to match $n_q$.
Performance peaks at $n_q=4$: too few qubits ($n_q=2$) provides insufficient
expressibility for 5-task CL, while more qubits ($n_q=6$) reintroduce barren
plateau problems and increase circuit depth relative to the available training
signal.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{../figures/ablation_qubits.pdf}
  \caption{Effect of qubit count on AA. $n_q=4$ is optimal, balancing
           Hilbert space expressibility against barren plateau gradient
           vanishing.}
  \label{fig:abl_qub}
\end{figure}


% ─── Noise Robustness ─────────────────────────────────────────────────────────
\section{Noise Robustness Analysis}
\label{sec:noise}

A central concern for near-term quantum continual learning is whether the
performance advantage of QTCL degrades under realistic hardware noise.
We address this by simulating the IBM\,Brisbane 127-qubit processor noise model
using calibration data extracted via \texttt{qiskit-ibm-runtime}'s
\texttt{FakeBrisbane} backend, applied as a depolarizing channel on the
PennyLane \texttt{default.mixed} device.
All experiments in this section were executed on the Hercules HPC cluster
at Universidad Pablo de Olavide (48\,h wall time, 32\,CPU cores, 64\,GB RAM).

\subsection{Noise Model}

The noise model uses gate-specific depolarizing channel strengths derived
from the Brisbane calibration data.
For the 4-qubit QTCL circuit, the relevant parameters are:
a mean single-qubit gate error of $p_\mathrm{1q} = 2.91 \times 10^{-4}$
(per-qubit: $2.43 \times 10^{-4}$, $2.43 \times 10^{-4}$, $3.35 \times 10^{-4}$,
$3.43 \times 10^{-4}$) and a two-qubit gate error of $p_\mathrm{2q} = 10^{-2}$.
Readout errors are not modelled in this simulation.

\subsection{Results}

\begin{table}[t]
  \centering
  \caption{Noise robustness of QTCL (PennyLane) under the IBM\,Brisbane
           depolarizing noise model ($n=2$ seeds).
           $\Delta$ denotes the absolute change from clean to noisy simulation.}
  \label{tab:noise}
  \small
  \begin{tabular}{lccc}
    \toprule
    Condition & AA & F & BWT \\
    \midrule
    Clean simulation    & $0.866\pm0.009$ & $0.088\pm0.006$ & $-0.083\pm0.002$ \\
    Noisy (FakeBrisbane)& $0.840\pm0.072$ & $0.089\pm0.048$ & $-0.081\pm0.050$ \\
    \midrule
    $\Delta$ (noisy $-$ clean) & $-0.026$ & $+0.001$ & $+0.002$ \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:noise} shows the noise robustness results.
QTCL sustains an average accuracy of $0.840 \pm 0.072$ under the Brisbane
noise model, compared to $0.866 \pm 0.009$ in clean simulation.
The absolute accuracy degradation of $\Delta\mathrm{AA} = -0.026$ (2.6\,\%)
is modest and well within the range expected for 4-qubit shallow circuits
($L_s = 2$, $L_t = 1$) at current IBM gate fidelities.
The forgetting metric is essentially unchanged ($\Delta\mathrm{F} = +0.001$),
indicating that the anti-forgetting mechanisms are robust to depolarizing noise
at this circuit depth.

The increased standard deviation in the noisy condition (AA: $0.072$ vs.\
$0.009$) reflects shot-to-shot variance induced by the stochastic noise channel.
Averaging over more seeds or applying Zero-Noise Extrapolation would be expected
to reduce this variance in a full hardware deployment.

\subsection{Implications for Real-Hardware Execution}

The code provides drop-in IBM Quantum Runtime integration via
\texttt{EstimatorV2}: replacing \texttt{StatevectorEstimator} requires only
setting the environment variables \texttt{IBM\_QUANTUM=true},
\texttt{IBM\_QUANTUM\_TOKEN}, and \texttt{IBM\_BACKEND}.
The complete continual learning loop---including Fisher computation, rehearsal
buffer, and multi-task evaluation---operates on hardware without modification.
Based on the 2.6\,\% simulation-to-noisy-simulation gap, we project that
real-device execution on IBM\,Brisbane would achieve average accuracy
in the range 0.82--0.85 for the 4-qubit configuration, preserving the
qualitative advantage over classical baselines.


% ─── Discussion ───────────────────────────────────────────────────────────────
\section{Discussion}
\label{sec:discussion}

\subsection{Why Does QTCL Outperform Classical EWC?}

Three complementary mechanisms explain the observed $3.3\times$ forgetting
reduction in the PennyLane QTCL setting.

\noindent\textit{Compact quantum parameter space.}
The variational quantum circuit has 36 trainable parameters vs.\
$\approx 25{,}000$ for the classical encoder.
The ratio $\lambda / p$ (regularization per parameter) is much higher for
the quantum component, making Elastic Weight Consolidation more effective per
unit of regularization---each gradient update affects a larger fraction of
the total quantum parameter space.

\noindent\textit{Quantum Hilbert space geometry.}
The bounded $[-1,+1]$ expectation value outputs restrict the output manifold,
preventing the weight blow-up that can occur in classical models during
sequential training.
This bounded geometry contributes to the lower forgetting observed even in
Qiskit Naive relative to PennyLane Naive.

\noindent\textit{Rehearsal anchors the quantum optimization landscape.}
Quantum Elastic Weight Consolidation alone (average accuracy 0.617) is barely
competitive with Classical Elastic Weight Consolidation (0.636).
Adding rehearsal yields a $+0.227$ average accuracy gain.
We attribute this to the barren plateau phenomenon (Eq.~(\ref{eq:barren})):
once a quantum circuit drifts from a previous-task solution, the vanishing
gradient makes recovery exponentially difficult.
Rehearsal samples inject gradient signals pointing back toward past solutions
before the circuit drifts into a flat gradient region, while Elastic Weight
Consolidation reduces the drift rate.
The two mechanisms are complementary and their combined effect exceeds the
sum of individual contributions.

\subsection{PennyLane vs.\ Qiskit}

The observed accuracy gap between QTCL (PennyLane, 0.844) and QTCL-Qiskit
(0.632) is primarily attributable to the gradient computation method.
Adjoint differentiation computes exact gradients with a single backward pass
through the statevector, while the parameter-shift rule requires two circuit
evaluations per parameter (72 evaluations for the 36-parameter PennyLane
circuit).
This introduces stochastic variance per gradient step that limits effective
convergence in 25 training epochs, effectively acting as gradient noise
that prevents the optimizer from reaching high-accuracy solutions.

Conversely, the parameter-shift gradient noise provides an implicit forgetting
barrier: noisy gradient steps are unlikely to move parameters far from
previous-task solutions, explaining why Qiskit Naive (forgetting 0.038) far
outperforms PennyLane Quantum Naive (forgetting 0.462).

This trade-off has a direct hardware implication.
On real quantum processors, all gradient computation uses parameter-shift
(or finite differences), so the Qiskit low-forgetting regime better reflects
the expected hardware behaviour.
The 2.6\,\% accuracy gap between clean and noisy simulation
(Section~\ref{sec:noise}) suggests that reducing gradient variance via
increased shot counts or gradient aggregation techniques would recover
a significant fraction of the PennyLane accuracy advantage on hardware.

\subsection{Limitations}

\noindent\textit{Benchmark scale.}
Split-MNIST with 150 training samples per task is a controlled evaluation.
Scaling to Split-CIFAR-10 or ImageNet subsets requires deeper circuits and
more qubits, which exacerbates barren plateaus.

\noindent\textit{Rehearsal assumption.}
Experience replay requires storing raw samples, which may be privacy-sensitive.
Generative rehearsal via quantum circuit synthesis is a natural extension.

\noindent\textit{Task-incremental setting.}
We assume task identities are available at test time.
Extending to class-incremental continual learning requires an additional
task-inference mechanism.

\noindent\textit{Noise model approximation.}
The FakeBrisbane noise model does not capture readout errors, crosstalk, or
time-varying calibration drift present on real hardware.
Full validation on physical IBM\,Brisbane would quantify these additional effects.


% ─── Related Work ─────────────────────────────────────────────────────────────
\section{Related Work}
\label{sec:related}

\textbf{Classical continual learning.}
EWC~\cite{kirkpatrick2017overcoming} and SI~\cite{zenke2017continual}
are the canonical regularization approaches.
iCaRL~\cite{Rebuffi2017} introduced prototype rehearsal for class-incremental
learning.
Comprehensive reviews are given by Parisi et al.~\cite{parisi2019continual}.

\textbf{Quantum machine learning.}
Biamonte et al.~\cite{biamonte2017} and Schuld and Killoran~\cite{schuld2019}
establish QML foundations.
Cerezo et al.~\cite{cerezo2021variational} review VQAs.
Havlíček et al.~\cite{havlicek2019} demonstrate quantum kernel classification
on real hardware.
Schuld et al.~\cite{schuld2021effect} analyze data encoding and expressibility.
Barren plateaus are characterized in~\cite{mcclean2018barren}.

\textbf{Quantum continual learning.}
Chen~\cite{chen2022quantumcl} is among the first to explore QCL but does not
apply Fisher EWC to VQC parameters.
Martínez~\cite{danimap27qcl} demonstrates quantum CL on Split-CIFAR-10 with
a pretrained classical backbone, achieving AA~$=89.2\%$ using Qiskit hybrid
models---a complementary approach that uses frozen classical features rather
than end-to-end training.
Our work differs by training the full model end-to-end, creating realistic
catastrophic forgetting, and applying joint quantum-classical Fisher EWC.

\textbf{Transfer learning in QML.}
Mari et al.~\cite{mari2020transfer} study quantum transfer learning with
frozen classical encoders, finding that classical pre-training improves
quantum kernel performance on related tasks.
QTCL uses a trainable (not frozen) encoder to avoid trivial separability.


% ─── Conclusion ───────────────────────────────────────────────────────────────
\section{Conclusion}
\label{sec:conclusion}

We presented QTCL, a hybrid quantum-classical continual learning framework
combining end-to-end trainable classical encoding, variational quantum circuits
with task-shared and task-specific parameter banks, Fisher Information-based
Elastic Weight Consolidation applied jointly to classical and quantum parameters,
and experience replay.
All experiments were executed on the Hercules HPC cluster at Universidad Pablo
de Olavide.
On Split-MNIST with 3 independent seeds, QTCL achieves average accuracy
$0.844 \pm 0.012$ and forgetting $0.130 \pm 0.023$ with the PennyLane backend,
a $+20.8$ percentage-point accuracy improvement and $3.3\times$ forgetting
reduction over the best classical baseline.
A noise robustness analysis under the IBM\,Brisbane calibration noise model
shows only a 2.6\,\% absolute accuracy degradation, confirming compatibility
with near-term quantum hardware.

The dual-backend evaluation reveals an important framework effect:
PennyLane adjoint differentiation enables high accuracy (0.844) while Qiskit
parameter-shift gradients produce implicit forgetting resistance (0.007)
at moderate accuracy (0.632), reflecting fundamentally different noise-accuracy
trade-offs that will be informative for real-hardware deployment.

The ablation study establishes that rehearsal is the critical component
activating the quantum advantage, the optimal regularisation strength for
quantum models lies in a lower range than for classical models,
shallow circuits ($L_s=2$) are optimal due to barren plateau gradient vanishing,
and 4 qubits is the optimal count for this task scale.

Future work will extend QTCL to Split-CIFAR-10 with larger variational quantum
circuits and barren plateau mitigation techniques such as layer-wise training,
explore quantum generative rehearsal to eliminate raw sample storage, and study
noise-robust Fisher estimation for hardware Elastic Weight Consolidation.

\begin{acknowledgments}
The author thanks the PennyLane and Qiskit development teams for their
open-source quantum simulation tools.
Simulations were executed on the Hercules high-performance computing cluster
of the Centro de Investigación y Tecnología en Ciencias y Humanidades (CITIUS)
at Universidad Pablo de Olavide, Sevilla, Spain.
Preliminary prototyping was carried out on a local workstation (Intel i5-8500,
Ubuntu~24.04, 16\,GB RAM).
\end{acknowledgments}

\bibliographystyle{quantum}
\bibliography{references}


% ─── Appendix ─────────────────────────────────────────────────────────────────
\appendix

\section{Reproducibility}
\label{app:repro}

\textbf{Software versions}: Python~3.11, PennyLane~0.43+,
\texttt{pennylane-lightning}~0.43+, PyTorch~2.0+, torchvision,
Qiskit~2.3.1, \texttt{qiskit-machine-learning}~0.9.0,
\texttt{qiskit-ibm-runtime}~0.45+.

\noindent\textbf{Local execution (PennyLane main + ablations + noise):}
\begin{verbatim}
python3 code/qtcl_v6_experiment.py         # main PennyLane results
python3 code/qtcl_qiskit_experiment.py     # Qiskit backend results
python3 code/qtcl_noise_experiment.py      # FakeBrisbane noise analysis
\end{verbatim}

\noindent\textbf{Hercules HPC (SLURM, all results reproduced here):}
\begin{verbatim}
sbatch hercules/submit_quantics_main.slurm  # main + ablation lambda + rho
sbatch hercules/submit_noise.slurm          # IBM FakeBrisbane noise analysis
\end{verbatim}
The Hercules jobs use 24--32\,CPU cores and 48--64\,GB RAM on the
\texttt{standard} partition.
Environment setup: \texttt{module load Miniconda3 \&\& conda activate qtcl}.

\noindent\textbf{IBM Quantum hardware (drop-in):}
\begin{verbatim}
export IBM_QUANTUM=true
export IBM_QUANTUM_TOKEN=<token>
export IBM_BACKEND=ibm_brisbane
python3 code/qtcl_qiskit_experiment.py
\end{verbatim}

\noindent\textbf{Hyperparameters}: all in the top-level constants of each
script. Default configuration matches Table~\ref{tab:hp}.

\noindent Code, figures, and pre-computed results:
\url{https://github.com/danimap27/qtcl-paper}

\section{Classical Baseline Details}
\label{app:classical}

The classical encoder architecture is identical to the QTCL encoder:
$784\xrightarrow{\text{Linear}}\xrightarrow{\text{ReLU}}\xrightarrow{\text{LN}}32
\xrightarrow{\text{Linear}}\xrightarrow{\text{Tanh}}4$.
The classical head is $4\xrightarrow{\text{Linear}}\xrightarrow{\text{ReLU}}
8\xrightarrow{\text{Linear}}2$.
Total parameters: $\approx$25{,}000 (encoder: 24{,}932; head: 58).
This ensures the comparison is fair: both models have the same input
representation capacity.

\section{Quantum Circuit Parameters}
\label{app:circuits}

\textbf{PennyLane} \texttt{StronglyEntanglingLayers}:
each layer applies $(R_Z, R_Y, R_Z)$ to each qubit (3 parameters per qubit
per layer) plus entangling CNOT gates with a pattern that shifts by 1 qubit
per layer (layer $l$ uses range $[0,l\bmod n_q]$).
Total: $(L_s+L_t)\times n_q \times 3 = 3\times4\times3=36$ parameters.

\textbf{Qiskit} \texttt{EfficientSU2}:
applies $(R_Y, R_Z)$ rotation blocks to each qubit per rep plus CX entanglement.
Parameters: $n_q\times2\times(\text{reps}+1)$ per block.
Shared ($L_s=2$ reps): $4\times2\times3=24$; Task ($L_t=1$ rep): $4\times2\times2=16$.
Total: 40 parameters.

\begin{thebibliography}{99}
\bibitem{danimap27qcl}
D.~Mart\'inez,
``Quantum Continual Learning,''
\url{https://github.com/danimap27/Quantum_Continual_Learning}, 2024.

\bibitem{ba2016layer}
J.~L. Ba, J.~R. Kiros, and G.~E. Hinton,
``Layer Normalization,''
\emph{arXiv:1607.06450}, 2016.

\bibitem{haug2021}
T.~Haug and M.~S. Kim,
``Scalable and accurate variational Bayes for high-dimensional binary regression,''
\emph{arXiv:2109.11377}, 2021.

\bibitem{jones2020}
T.~Jones and J.~Gacon,
``Efficient calculation of gradients in classical simulations of variational quantum algorithms,''
\emph{arXiv:2009.02823}, 2020.

\bibitem{li2017efficient}
Y.~Li and S.~C. Benjamin,
``Efficient variational quantum simulator incorporating active error minimization,''
\emph{Physical Review X}, 7(2), 021050, 2017.
\end{thebibliography}

\end{document}
